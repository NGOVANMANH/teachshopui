import clsx from "clsx";
import styles from './Footer.module.scss';

const Footer = () => {
    return (
        <div className={clsx("Footer bg-main", styles.height10rem)}>
            Footer
        </div>
    );
}

export default Footer;
